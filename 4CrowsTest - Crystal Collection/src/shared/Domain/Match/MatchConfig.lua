local Types = require(script.Parent.MatchTypes)

local MatchConfig: Types.MatchConfigTypes = {
	GameplayTime = 60,
	IntermissionTime = 10,
	MaxSpawnedCrystals = 10,
	CrystalSpawnInterval = 2,
}

return MatchConfig
